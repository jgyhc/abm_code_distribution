{
  "name": "abm-code-distribution",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Cloudflare Workers demo",
  "scripts": {
    "build": "npm run check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "import:abm": "node ./scripts/import-abm-codes.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260410.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.11.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
