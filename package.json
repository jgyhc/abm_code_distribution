{
  "name": "abm-code-distribution",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Cloudflare Workers demo",
  "scripts": {
    "build": "npm run check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260410.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.11.0"
  }
}
